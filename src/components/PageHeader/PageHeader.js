import React from 'react';
import { Layout } from 'antd';
const PageHeaders = () => {
	const { Header } = Layout;
	return (
		<Header className="header">
			<div className="textHeader">SOLICITUDES</div>
		</Header>
	);
};
export default PageHeaders;
